# 🚀 Быстрый старт бэкенда маркетплейса

## Шаг 1: Установка MongoDB

Убедитесь, что MongoDB установлена и запущена на вашем компьютере.

**Windows:**
- Скачайте и установите MongoDB с официального сайта: https://www.mongodb.com/try/download/community
- Запустите MongoDB:
```bash
mongod
```

**macOS (через Homebrew):**
```bash
brew tap mongodb/brew
brew install mongodb-community
brew services start mongodb-community
```

**Linux:**
```bash
sudo apt-get install mongodb
sudo systemctl start mongodb
```

## Шаг 2: Настройка переменных окружения

Создайте файл `.env` в папке `server/` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:
```env
PORT=5000
MONGO_URI=mongodb://localhost:27017/marketplace
JWT_SECRET=замените_на_свой_случайный_секретный_ключ
NODE_ENV=development
```

⚠️ **Важно:** Измените `JWT_SECRET` на случайную строку для безопасности!

## Шаг 3: Установка зависимостей

```bash
cd server
npm install
```

## Шаг 4: Заполнение базы данных тестовыми данными (опционально)

```bash
npm run seed
```

Это создаст тестовых пользователей и товары:
- **Админ:** login=`admin`, password=`admin123`
- **Продавец:** login=`seller1`, password=`seller123`
- **Покупатель:** login=`buyer1`, password=`buyer123`

## Шаг 5: Запуск сервера

**Режим разработки (с автоперезагрузкой):**
```bash
npm run dev
```

**Продакшн режим:**
```bash
npm start
```

Сервер запустится на http://localhost:5000

## 🧪 Тестирование API

Вы можете протестировать API с помощью:
- **Postman** - https://www.postman.com/
- **Thunder Client** (расширение VS Code)
- **curl** (командная строка)

### Примеры запросов:

**Регистрация:**
```bash
POST http://localhost:5000/api/auth/register
Content-Type: application/json

{
  "login": "testuser",
  "email": "test@example.com",
  "password": "test123"
}
```

**Авторизация:**
```bash
POST http://localhost:5000/api/auth/login
Content-Type: application/json

{
  "login": "testuser",
  "password": "test123"
}
```

**Получить все товары:**
```bash
GET http://localhost:5000/api/products
```

**Получить популярные товары:**
```bash
GET http://localhost:5000/api/products/popular
```

## 📋 Следующие шаги

1. Убедитесь, что MongoDB запущена
2. Создайте файл `.env`
3. Установите зависимости
4. (Опционально) Заполните базу тестовыми данными
5. Запустите сервер
6. Протестируйте API

## 🔗 Подключение к фронтенду

В вашем React приложении (`client/`) используйте базовый URL API:
```javascript
const API_URL = 'http://localhost:5000/api';
```

Для запросов с аутентификацией добавляйте токен в заголовок:
```javascript
headers: {
  'Authorization': `Bearer ${token}`,
  'Content-Type': 'application/json'
}
```

## ❗ Возможные проблемы

**Ошибка подключения к MongoDB:**
- Убедитесь, что MongoDB запущена
- Проверьте правильность `MONGO_URI` в `.env`

**Ошибка порта занят:**
- Измените порт в `.env` файле
- Или остановите процесс на порту 5000

**Ошибка JWT:**
- Убедитесь, что `JWT_SECRET` установлен в `.env`

## 📚 Документация API

Полная документация доступна в файле `README.md`

